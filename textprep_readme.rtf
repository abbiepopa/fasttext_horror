textprep.sh takes two arguments. The first is the name of the file you want to prep, and the second is the name of the label you want to apply to each line. For example:

sh textprep.sh test.txt test

it outpus three files, "fin" is the file shich should be renamed for use with your model, presumably after concatenation with other training data
